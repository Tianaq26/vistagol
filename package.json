{
  "name": "vistagol",
  "version": "0.1.0",
  "type": "module",
  "description": "Boletería con visualización 3D del estadio: ve tu asiento antes de comprarlo.",
  "author": "Sebastián Aguilar Quesada",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=iife --minify --outfile=app.js",
    "dev": "npm run build && npx http-server -p 8080 -c-1"
  },
  "dependencies": {
    "three": "0.160.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
